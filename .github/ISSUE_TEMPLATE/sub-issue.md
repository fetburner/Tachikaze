---
name: sub-issue
about: エピックを分割した実装単位。単独で完結させる
title: "[E?-?] "
---

**このタスクは単独で完結します。** 下の「読む文書」以外の docs / issue を開く必要はありません。実装前に必ず「罠」を読むこと。

親: #

<!-- 記入したら、この説明コメントはすべて削除する。上の 2 行はそのまま残す。

     「単独で完結します」は建前ではなく要件。**エピックや他の issue を読まないと実装できない
     サブ issue は、分割が失敗している。** エピックの「方針」で決まったことは、参照させずに
     必要な行をここへ転記する。分割の目的は、実装する人が余計な文書を読まずに済むこと。 -->

## 並列

- **先行**: <!-- マージ済みでないと着手できない issue。なければ「なし」 -->
- **同時可**: <!-- 並行して進められる issue -->
- **触るファイル**: <!-- 実際に編集するファイルを全部列挙する。新規作成は「新規」と書く。
                       同じファイルを触る issue があるなら、どちらを先にマージするか明示する -->

他の issue のブランチと同じファイルを触らないよう、上の「触るファイル」から出ないこと。必要になったら先にその旨をコメントする。

## 読む文書

<!-- **必要最小限**。文書名だけでなく節名・関数名まで指定して、丸ごと読ませない。
     ここに挙げなかった文書は開かせない。これがサブ issue に分割している目的（コンテキストの節約）。
     CLAUDE.md は常に読まれるので、罠 1〜4 は番号で参照してよい。 -->

## 解くべき問題

<!-- なぜこれが必要か、現状の何が困るか。実装方法ではなく問題を書く。
     「方針は変えない」ものがあるなら明示する（例: .dtvi が必須である理由は変えない）。 -->

## やること

<!-- 番号付きの手順。判断が要る箇所は「決めて doc comment に理由を書く」と明示する
     （判断の根拠はコードに残らないため）。 -->

1. 

## 罠

<!-- **実際に壊れたもの**と、間違えても例外が飛ばない箇所だけ。仮説は書かない。
     「変えてはいけない既存の性質」もここに書く（守っている既存テスト名を添えると強い）。 -->

## 完了条件

<!-- - [ ] のチェックリスト。テストで固定されるものと、目視・実測で確認するものを分けて書く。 -->

- [ ] 
